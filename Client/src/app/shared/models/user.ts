export interface User {
    id: string;
    email: string;
    userName: string;
    token: string;
}

export interface Address {
    firstName: string;
    lastName: string;
    city: string;
    zipcode: string
}